# frontend

Vite frontend initialized with a React + TypeScript starter.

## Commands

- `npm run dev` starts the development server
- `npm run build` creates a production build
- `npm run preview` previews the production build locally